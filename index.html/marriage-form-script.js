document.getElementById("marriageForm").addEventListener("submit", function(event) {
    event.preventDefault(); // Prevents the form from submitting the default way
    alert("Your application for Ekram's marriage form has been submitted successfully!");
    // Code to handle form data submission can be added here
});